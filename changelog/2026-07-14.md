# 2026-07-14

## 论文式遥控模式

- 根据 BDX 论文附录 C / 表 VII，将“摇杆回零自动切站立”的初步方案改为显式遥控模式：遥控
  请求默认 standing，起立后进入 standing policy；`R1` 短按切换 standing/walking，walking 中长按 `R1` 将速度
  增益从 `50%` 提高到 `100%`，`A` 明确请求 standing。摇杆回中只表示 walking 零速度，
  不自动改变策略。
- 新增 `sim2sim/gamepad_input.py`，直接读取 Linux `/dev/input/js*` 并在后台发布一致快照。设备
  未连接或运行中拔出时输出全零命令，并把期望模式复位为 standing。
- 站立映射：左摇杆控制 torso pitch/yaw/height，并反向补偿头部以尽量保持 gaze；右摇杆控制
  head pitch/yaw，在摇杆后 `25%` 行程逐渐加入 torso 转动扩展视线；方向键控制 head height/roll；
  `L2/R2` 控制 torso roll。
- 行走映射：左摇杆控制 longitudinal velocity/turning rate，`L2/R2` 控制 lateral velocity，
  右摇杆和方向键继续控制头部。所有输出限制在当前训练命令域内。
- `config/oceanbdx.yaml` 新增 `puppeteering` 配置，包含死区、R1 短按/长按时长、两档速度增益、
  gaze→torso 分段阈值和本项目手柄实测 axis/button 编号。`walk_button_behavior=hold` 可选用
  “按住才走、松开回站立”语义，默认 `toggle` 严格对齐论文。

## 模式切换与临界状态

- 手柄模式请求只在 200Hz 控制线程处理，不由设备线程直接修改策略状态。`R1` 停走复用既有
  `WALK_DECEL → WAIT_DOUBLE_SUPPORT → FINAL_DECEL → ZERO_HOLD → RL_STAND`，因此命令归零
  时机器人尚有惯性、仍在单支撑或 IMU/关节未稳定，都不会直接切 standing policy。
- walking→standing 过渡中重新请求 walking，会在一个控制周期内取消停站，并由当前 effective
  command 按限加速度恢复；不会跳回旧速度。
- standing→walking 保留当前摇杆速度目标，不再被 `_begin_stand_to_walk()` 或最终模型切换清零；
  effective command 仍从零爬升。站立 neutral 且已连续稳定时使用预确认快速路径，在下一 policy
  boundary 切模型；处于下蹲/扭身姿态时仍先由 standing policy 最长 `1.5s` 平滑回 neutral。
- 手柄触发的切换若超时，会锁存对应失败目标，避免每 `3.5s/5s` 循环请求和刷日志；改变模式、
  显式 `A` 或手柄断连边沿可解除锁存并重试一次。缺少目标 ONNX
  时也拒绝手柄自动切换，不回退到错误模型语义。
- 模型切换仍传递两帧动作历史、当前腿/脖子 target、FOH/LPF 与 path frame；没有混合两套 raw action。
- 切换期间摇杆按当前实际运行的 policy 解释，而不是按尚未完成的模式请求提前改变语义。
  walking→standing 完成后，左摇杆与 `L2/R2` 必须连续回中 `0.15s` 才重新使能 torso 控制，
  避免未回中的速度命令变成站立前倾/侧倾命令。
- `A` 设为无条件 standing 高优先级，并消费重叠的 `R1` 手势；可选 `hold` 模式固定使用配置的
  `50%` 保守增益，松开后不会把同一组轴立即重解释为 standing posture。
- 对照 `test_gamepad.cpp` / `gamepad_driver.hpp` 的实测枚举，将 `START=button[11]` 映射为键盘
  `1`：`SIT` 中启动 `STAND_UP`，walking 中复用安全停站流程，standing 中保持当前模型。

## 输入安全与验证

- 移除 `RunLogger` 每 `0.3s` 输出一次的 `[run]` 终端状态行，避免 sim2sim 运行时刷屏；
  `runlog/sim2sim_*.csv` 仍按 policy step 完整记录观测、动作和目标关节角。
- C++ `GamepadDriver::GetState()` 在设备离线时改为返回全零快照，并给双缓冲发布/复制增加互斥，
  避免未来接入真机主控后在断连或连续事件期间读到旧值/数据竞争；同步修正 `types.hpp` 映射注释。
- 新增 `sim2sim/test_gamepad_mapping.py` 14 项测试，覆盖默认站立、断连清零、R1 短按/长按、
  A/R1 重叠优先级、START 边沿、YAML/实测键位一致性、跨 policy 轴语义和可选 hold 模式。
- `sim2sim/test_walk_stand_switch.py` 扩展到 27 项，覆盖 walking 零摇杆不换模式、R1 安全停站、
  START 起立/安全停站、未回中切站、站转走命令保留/限加速度、超时锁存和 `A` 人工重试。
- `mujoco_sim.py` 支持脚本与 `python -m sim2sim.mujoco_sim` 两种启动方式；相关 Python 回归共
  `46` 项全部通过，C++ 全量目标重新构建通过。
- 当前实际 stand `77→14` / walk `80→14` ONNX 的 MuJoCo headless 闭环：稳定 standing 请求
  walking 后在下一策略边界切换，约 `0.16s` 后 effective vx 跨过步态阈值；walking 请求停止后
  约 `1.52s` 完成减速、双支撑等待和稳定确认并进入 standing，最终保持直立。
- 最大下蹲命令 `h=-0.04m` 时请求 walking，会先执行 `1.5s` torso recenter，再连续稳定确认，约
  `2.01s` 进入 walking；当前速度目标完整保留，闭环未进入 `DAMPING`。
- YAML 沿用此前通过 `build/test_gamepad` 实机确认的轴方向和按键编号；本次会话没有检测到
  `/dev/input/js0`，因此只完成映射一致性与状态机回归，没有重复采集真实手柄事件流。

## 部署边界

- 本轮完整遥控映射与双模型状态机位于 Python `sim2sim/mujoco_sim.py --gamepad`。C++ 真机主控仍是
  旧单模型观测/动作接口，尚不能部署 stand77/walk80 双模型；`--real` 的策略观测也仍来自 MuJoCo。
