# 2026-07-12

## 行走策略头命令范围同步

- 同步 OceanIsaacLab `2026-07-12_18-06-48` 训练诊断后的硬件适配范围：
  `max_head_dh=0.007m`、`max_head_pitch=0.17rad`、`max_head_yaw=0.33rad`、
  `max_head_roll=0.20rad`。
- 同时更新 `sim2sim/mujoco_sim.py` 的缺省回退值，确保未提供 YAML 字段时也不会
  向策略发送旧的超训练分布头命令。
- 未修改现有 `policy/policy.onnx` 与 `policy/policy.onnx.data`；需要等待修改后的
  OceanIsaacLab 策略训练完成并重新导出。
