# 2026-07-12

## 行走策略头命令范围同步

- 同步 OceanIsaacLab `2026-07-12_18-06-48` 训练诊断后的硬件适配范围：
  `max_head_dh=0.007m`、`max_head_pitch=0.17rad`、`max_head_yaw=0.33rad`、
  `max_head_roll=0.20rad`。
- 同时更新 `sim2sim/mujoco_sim.py` 的缺省回退值，确保未提供 YAML 字段时也不会
  向策略发送旧的超训练分布头命令。
- 未修改现有 `policy/policy.onnx` 与 `policy/policy.onnx.data`；需要等待修改后的
  OceanIsaacLab 策略训练完成并重新导出。

## StandPaper 双模型切换同步

- Python MuJoCo sim2sim 的站立/行走接口更新为 `77/80` 维观测、统一 14维动作；旧 74维站立
  和 77维行走 ONNX 会被维度检查拒绝。现有策略文件未覆盖，需等待 OceanIsaacLab 重训导出。
- 两策略切换时传递 `a_(t-1)`、`a_(t-2)`，保留当前腿/脖子 setpoint 及 FOH/37.5Hz 低通
  状态，不再在切换时清零动作历史或把目标重置为实测 q。
- `RL_WALK → RL_STAND` 改为异步请求，在控制周期边界处理，并等待下一次连续确认的双支撑；
  零速已双脚着地可直接切换，超过 2 秒未确认则取消。stand→walk 在控制周期边界直接切换。
- path frame 站立分支同步训练侧，使用去除左右脚 link-frame 固有 yaw 偏置后的双脚平均
  heading；path-frame 位置/FK 使用末端脚 link body origin/quaternion，而 sole geom center 仅用于
  接触判定，修复 neutral pose 约 `2.585cm` 的训练/部署位置偏差。
- 站立 torso 限幅同步训练可行域：h `[-0.04,+0.01]m`、pitch `±0.17rad`、yaw `±0.24rad`、
  roll `±0.09rad`，高度改为显式非对称 min/max。
- walk→stand 使用切换瞬间实测躯干高度和姿态初始化 torso 命令，不再硬归零；
  `STAND_UP→RL_STAND` 也从实测躯干姿态起步，再用 `0.5s` 余弦接管平滑回用户/neutral
  命令，同时渐入腿/脖子策略目标并完成 `50/3 → 10/0.3` 增益过渡，避免新目标在脚本
  高增益下产生启动冲击。
- 站立和行走统一使用附录 B 腿部软件 PD `kP=10`、`kD=0.3`。
- 腿与脖子目标均在 200Hz 主循环内执行 FOH + 37.5Hz 低通，切换时保持滤波连续。

## 验证与部署边界

- 轻量 sim2sim 测试通过 stand/walk `77/80` 维观测、动作历史传递、控制周期边界、当前/下一
  双支撑、零速冻结、超时取消逻辑；q=0 双脚 heading 校准误差为 0。
- 真实 MuJoCo 使用现有 80维 walking policy 作为 standing fallback 回归：首个 RL 控制步
  applied 腿目标跳变从 `0.09057rad` 降到 `2.24e-5rad`，腿力矩跳变从 `4.591Nm` 降到
  `0.079Nm`；完整 `0.5s` 接管窗最大单步目标/力矩变化为 `0.00681rad/0.175Nm`，10秒末
  仍保持 `RL_STAND` 双脚支撑。该测试不能替代新版 77维 standing policy 的闭环验收。
- Python sim2sim 已支持论文双模型切换；C++ 真机主控仍是旧 41维、10动作、单策略链路。
- `mujoco_sim.py --real` 当前只把 MuJoCo 生成的目标经桥下发，policy 观测不是来自真机状态
  估计器，因此不是完整真机闭环，不能据此宣称真机无缝切换已经完成。
