# 2026-07-11

## model_39200 sim2sim 逐脚诊断

- 确认部署的 `policy/policy.onnx(.data)` 与训练 run
  `2026-07-10_18-46-07` 导出文件 SHA-256 完全一致。
- 新增 `sim2sim/diag_walk_policy.py`：无界面执行正常 SIT -> STAND_UP -> RL_STAND ->
  RL_WALK 流程，固定速度命令并统计左右脚接触率、脚掌 box 世界系最低点净间隙、
  腿部力矩和脖子关节/目标 RMS。
- `vx=0.15`：速度 `0.151m/s`，右/左脚净间隙 p50 为 `0.68/0.01cm`，力矩 p99
  为 `6.74/6.27Nm`，远低于训练和部署共同使用的 `23.7Nm` 上限。
- `vx=-0.15`：速度几乎为 0，右/左接触率为 `99.8%/100%`；力矩 p99 仍只有
  `4.75/4.68Nm`。后退失败不是 MuJoCo 电机力矩不够，而是策略在 IsaacLab 中已经
  缺少稳定的后退摆动，sim2sim 进一步压缩了离地裕量。
- 训练侧已把参考抬脚提高到 5cm、保持接触权重 3.0、将周期头部起伏降到 1/3，
  并恢复论文 Table I neck 权重。需要重新训练并导出 ONNX 后才能在本工程体现。
- 当前工作树中已有的 `policy/policy.onnx` 和 `policy/policy.onnx.data` 未被本轮修改。
